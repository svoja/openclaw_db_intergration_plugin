{
  "id": "db-mysql",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "envPath": {
        "type": "string",
        "description": "Path to .env file with DB_HOST, DB_PORT, DB_USER, DB_PASSWORD, DB_NAME"
      }
    }
  },
  "uiHints": {
    "envPath": {
      "label": "Path to .env file",
      "placeholder": "/opt/myapp/.env"
    }
  }
}
